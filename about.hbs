===============================================================================
Third-party notices: crates linked into this binary
===============================================================================

ya-lsp is built from the Rust crates listed below and links them into the
binary. Their licences require their notices to be reproduced in distributions,
so all of them are reproduced here in full.

This section is generated by `cargo about` from the dependency graph, for every
platform ya-lsp is released for. Do not edit it by hand — see about.toml.

A crate that publishes no licence file of its own leaves `cargo about` nothing
to harvest, and the SPDX template is reproduced in its place — recognisable by
its unfilled "Copyright (c) <year> <owner>" line. `ruby-rbs` and `ruby-rbs-sys`
are the two such crates here; their copyright notice is in NOTICE.txt, which
ships beside this file and is reproduced by `ya-lsp --licenses`.

Summary:
{{#each overview}}
  {{{name}}} — {{count}} crate(s)
{{/each}}
{{#each licenses}}

-------------------------------------------------------------------------------
{{{name}}}
-------------------------------------------------------------------------------

Used by:
{{#each used_by}}
  {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} — {{{crate.repository}}}{{/if}}
{{/each}}

{{{text}}}
{{/each}}
