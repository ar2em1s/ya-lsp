{
  "name": "yalsp",
  "displayName": "ya-lsp",
  "description": "A standalone Ruby language server. No Ruby runtime required.",
  "version": "0.5.0",
  "license": "MIT",
  "icon": "images/logo.png",
  "publisher": "ar2em1s",
  "repository": {
    "type": "git",
    "url": "https://github.com/ar2em1s/ya-lsp.git",
    "directory": "editors/vscode"
  },
  "categories": [
    "Programming Languages"
  ],
  "keywords": [
    "ruby",
    "language server",
    "lsp"
  ],
  "engines": {
    "vscode": "^1.108.0"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:ruby",
    "onLanguage:erb"
  ],
  "contributes": {
    "configuration": [
      {
        "title": "ya-lsp",
        "order": 1,
        "properties": {
          "ya-lsp.serverPath": {
            "type": "string",
            "default": "",
            "markdownDescription": "Run a `ya-lsp` binary of your own instead of the one bundled with this extension — point it at `target/release/ya-lsp` to develop against a local build. Leave empty for the bundled one. `~` and `${workspaceFolder}` are expanded. Changing this restarts the server.",
            "scope": "machine-overridable",
            "order": 1
          },
          "ya-lsp.logLevel": {
            "type": "string",
            "enum": [
              "off",
              "error",
              "warn",
              "info",
              "debug",
              "trace"
            ],
            "enumDescriptions": [
              "Nothing at all.",
              "Only what stopped working.",
              "Also what was skipped or guessed — an unreadable file, a gem that could not be found.",
              "Also what it did: which Ruby it picked, how many files it indexed, how long that took.",
              "Also the work behind each request. Verbose enough to slow a large workspace down.",
              "Everything, this crate and its libraries. For diagnosing the server itself."
            ],
            "default": "info",
            "markdownDescription": "How much the server writes to the **ya-lsp** output channel. Applied while the server runs, so changing it takes effect on the next request rather than restarting anything. A `YA_LSP_LOG` inherited from your shell outranks it, and so does `[log] level` in a workspace `ya-lsp.toml`.",
            "scope": "machine-overridable",
            "order": 2
          },
          "ya-lsp.log.file": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Also write the log to a file, so a bug report can carry one instead of a screenshot of an output channel. Off by default: a server you installed for hover should not start writing into your repository. Overridden by `[log] file` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          },
          "ya-lsp.log.filePath": {
            "type": "string",
            "default": "tmp/ya-lsp.log",
            "markdownDescription": "Where that file goes, relative to the workspace folder or absolute. Two windows on one project write to one file and each line says which process wrote it; nothing is ever truncated, so the session you are trying to report survives a restart. Overridden by `[log] file_path` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 4
          },
          "ya-lsp.log.fileLevel": {
            "type": "string",
            "enum": [
              "off",
              "error",
              "warn",
              "info",
              "debug",
              "trace"
            ],
            "enumDescriptions": [
              "Nothing at all.",
              "Only what stopped working.",
              "Also what was skipped or guessed — an unreadable file, a gem that could not be found.",
              "Also what it did: which Ruby it picked, how many files it indexed, how long that took.",
              "Also the work behind each request: which method, which document and position, how long it took and what it answered.",
              "Everything, this crate and its libraries. For diagnosing the server itself."
            ],
            "default": "debug",
            "markdownDescription": "How much goes in the file, which is its own setting rather than `#ya-lsp.logLevel#` — the reason to turn the file on is the per-request detail the output channel does not carry. Overridden by `[log] file_level` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 5
          },
          "ya-lsp.trace.server": {
            "type": "string",
            "enum": [
              "off",
              "messages",
              "verbose"
            ],
            "enumDescriptions": [
              "No LSP traffic is logged.",
              "One line per request and notification: the method and how long it took.",
              "The same, with each message's full JSON payload."
            ],
            "default": "off",
            "markdownDescription": "Log the LSP traffic between VS Code and the server. For debugging this extension — to read what the server itself found, use `#ya-lsp.logLevel#`.",
            "scope": "window",
            "order": 6
          }
        }
      },
      {
        "title": "Gems",
        "order": 2,
        "properties": {
          "ya-lsp.gems.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Follow go-to-definition, hover and completion into the project's gems, read straight from `Gemfile.lock` and the gem directories on disk. Off, only your own code is indexed: startup is a little faster and every answer about a gem is missing. Overridden by `[gems] enabled` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.gems.defaultGems": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Also index the ~40 gems that ship inside Ruby itself — `json`, `uri`, `forwardable`, `optparse`. Their directories under `gems/` are empty placeholders, so with this off `require \"json\"` leads nowhere and `JSON.parse` has nothing to offer. Overridden by `[gems] default_gems` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.gems.rubyVersion": {
            "type": "string",
            "default": "",
            "markdownDescription": "Index the gems belonging to this Ruby, as in `3.3.0`, when detection picks the wrong one. Leave empty to read it from `.ruby-version`, `.tool-versions` or `Gemfile.lock`. Overridden by `[gems] ruby_version` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          },
          "ya-lsp.gems.paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "markdownDescription": "Extra gem roots to search *before* the built-in table — each a directory containing `gems/`, which is what `gem env gemdir` prints. The escape hatch for a layout ya-lsp guesses wrong about: a container image, a Nix store path, a hand-rolled install. Relative paths resolve against the workspace folder. Overridden by `[gems] paths` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 4
          }
        }
      },
      {
        "title": "Ruby signatures (RBS)",
        "order": 3,
        "properties": {
          "ya-lsp.rbs.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Complete and hover `String`, `Array`, `Hash`, `Kernel` and the rest of Ruby's core, whose methods are written in C and appear in no Ruby source. Read from the `rbs` gem when one is installed, and from a copy inside ya-lsp when none is. Off, `\"\".upcase` has nothing to offer. Overridden by `[rbs] enabled` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.rbs.stdlib": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Also the standard library — `CSV`, `URI`, `Logger`, `OptionParser` and ~57 others. Adds about 3.5 ms to every request, so turn it off on a very large project that only needs the core classes. Overridden by `[rbs] stdlib` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.rbs.path": {
            "type": "string",
            "default": "",
            "markdownDescription": "Read signatures from this directory instead of looking for one — a directory holding `core/`, usually an unpacked `rbs` gem. Leave empty to find one automatically, falling back to the copy inside ya-lsp. Overridden by `[rbs] path` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          }
        }
      },
      {
        "title": "Types",
        "order": 4,
        "properties": {
          "ya-lsp.types.guessFromNames": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Answer from a receiver's own name when nothing else can — `@user` is a `User`, `person` is a `Person`. The only answer ya-lsp gives that is allowed to be wrong: it is always labelled as a guess in the hover card and on a completion row, and it never displaces a type the code states or one derived from a signature or an assignment. Turn it off to keep only checkable answers. Overridden by `[types] guess_from_names` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.types.structs": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Read `Struct.new` and `Data.define`, so the members they install complete and hover like any other. Plain Ruby, not Rails: off, `Point = Struct.new(:x)` has nothing to offer on `point.x`. Overridden by `[types] structs` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.types.annotations": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Read a Sorbet `sig` and a YARD `@return` as the type they declare, where the Ruby itself cannot say. Off, a method annotated and not signed hovers as unknown. Overridden by `[types] annotations` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          }
        }
      },
      {
        "title": "Inlay hints",
        "order": 5,
        "properties": {
          "ya-lsp.hints.blockParameters": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Draw what a block parameter holds, where the method the block was passed to declares it — the `story` in `stories.each do |story|`. Overridden by `[hints] block_parameters` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.hints.locals": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Draw what a local holds where it is assigned the result of a call — the `author` in `author = story.author`. Not where the assignment already names the class: `x = Story.new` and `x = 1` say it themselves. Overridden by `[hints] locals` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.hints.returns": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Draw what a method returns where a signature declares it and the Ruby cannot say so — from the project's `sig/`, a gem's, or a Sorbet `sig` or YARD `@return` in another file. Overridden by `[hints] returns` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          }
        }
      },
      {
        "title": "Problems",
        "order": 6,
        "properties": {
          "ya-lsp.diagnostics.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Show what ya-lsp finds while indexing in the Problems panel — files that do not parse, and constructs it could not follow. Off, nothing from ya-lsp appears there whatever `#ya-lsp.diagnostics.rules#` says. Overridden by `[diagnostics] enabled` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.diagnostics.rules": {
            "type": "object",
            "default": {},
            "properties": {
              "parse-error": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "The file does not parse — Prism's own syntax error, and the only rule here that is unambiguously a defect in your code. **Default: error.**"
              },
              "parse-warning": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "Prism's warnings about code that *does* parse: `assigned but unused variable`, a duplicated `when`, a literal in a condition. Set this to `off` when RuboCop or Standard already reports them — `Lint/UselessAssignment` covers most of the same ground. **Default: warning.**"
              },
              "dynamic-constant-reference": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "A constant path ya-lsp could not follow, because what is left of the `::` is not itself a constant — `foo::Bar`. Legal Ruby; the rule records why go-to-definition stops there. **Default: off.**"
              },
              "dynamic-singleton-definition": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "`class << expr` or `def expr.name` where the receiver is not a statically known constant, so the singleton methods never enter the index. Legal Ruby. **Default: off.**"
              },
              "dynamic-ancestor": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "A superclass or a mixin argument that is not a constant — `class Foo < base`, `include mixin_for(x)`. Legal Ruby and ordinary in metaprogramming: on one real project this rule alone would have put 384 warnings on working code, which is why it ships off. **Default: off.**"
              },
              "top-level-mixin-self": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "`include self` or `extend self` written at the top level, where there is no enclosing module for `self` to name. **Default: off.**"
              },
              "invalid-private-constant": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "`private_constant` somewhere it cannot take effect: at the top level, on a dynamic receiver, or with a non-symbol argument. Mostly genuine misuse, but the last case is ya-lsp giving up rather than a defect — hence a hint. **Default: hint.**"
              },
              "invalid-method-visibility": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "`private`, `protected`, `public` or `module_function` used in a way Ruby does not accept: at the top level, with `attr_*` arguments, with an explicit receiver, or `module_function` inside a class. Also covers a non-literal argument, which is ya-lsp giving up rather than a defect — hence a hint. **Default: hint.**"
              },
              "undefined-method-visibility-target": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "`private :name` where no such method was indexed. A real bug once the index is complete, and off until it is: every hit measured across three real projects was `private_class_method :new`, which is correct Ruby that rubydex flags only because it does not model `Class#new`. **Default: off.**"
              },
              "undefined-constant-visibility-target": {
                "type": "string",
                "enum": [
                  "off",
                  "error",
                  "warning",
                  "information",
                  "hint"
                ],
                "enumDescriptions": [
                  "Not reported at all.",
                  "Reported as an error.",
                  "Reported as a warning.",
                  "Reported as information.",
                  "Reported as a hint — a faint marker rather than a squiggle."
                ],
                "markdownDescription": "`private_constant :NAME` where no such constant was indexed. Off for the same reason as the method rule, and it also fires on a class whose superclass could not be resolved. **Default: off.**"
              }
            },
            "additionalProperties": {
              "type": "string",
              "enum": [
                "off",
                "error",
                "warning",
                "information",
                "hint"
              ],
              "enumDescriptions": [
                "Not reported at all.",
                "Reported as an error.",
                "Reported as a warning.",
                "Reported as information.",
                "Reported as a hint — a faint marker rather than a squiggle."
              ]
            },
            "markdownDescription": "How loudly each rule reports, keyed by the name shown in a problem's `code`. Only `parse-error` and `parse-warning` are statements about your code, and they are the only two on by default; the rest describe what the indexer could not follow, fire on legal Ruby, and ship `off` or `hint`. Set one to `off` when another tool already reports it. Overridden by `[diagnostics.rules]` in a workspace `ya-lsp.toml`.",
            "examples": [
              {
                "parse-warning": "off",
                "dynamic-ancestor": "warning"
              }
            ],
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.rubocop.hint": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Offer RuboCop's own extension once, in a project that has a `.rubocop.yml` or `rubocop` in its `Gemfile.lock`. ya-lsp never runs Ruby, so no cop offence and no autocorrect ever comes from it — RuboCop publishes a language server of its own, and it runs alongside ya-lsp rather than instead of it. Off, nothing is suggested and nothing else changes.",
            "scope": "resource",
            "order": 3
          }
        }
      },
      {
        "title": "Rails",
        "order": 7,
        "properties": {
          "ya-lsp.rails.enabled": {
            "type": "string",
            "enum": [
              "auto",
              "on",
              "off"
            ],
            "enumDescriptions": [
              "On when the folder holds config/application.rb, or Gemfile.lock locks railties.",
              "Always on, for a project detection does not recognise.",
              "Never. Nothing in this project is read as Rails."
            ],
            "default": "auto",
            "markdownDescription": "Bring Rails' conventions to this project: the schema, the model macros, the routes, mailers and jobs, and what a template can call. Off is about **answers rather than speed** — a project that is not Rails should not be told about Rails, and one with an `app/views/` gets a hover citing a controller that does not exist. Overridden by `[rails] enabled` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.rails.schema": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Read `db/schema.rb` or `db/structure.sql`, so a column completes and hovers with its type. Includes the macros that rename a table — `self.table_name=` and `isolate_namespace`. Overridden by `[rails] schema` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.rails.models": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Read the model macros: `belongs_to` and `has_many`, `enum`, `attribute`, `delegate`, `scope` and 17 more. This is also what types `Story.where(...)` as a relation rather than as nothing. Overridden by `[rails] models` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          },
          "ya-lsp.rails.routes": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Read `config/routes.rb`, so `stories_path` is a name your controllers and views can complete and jump to. Overridden by `[rails] routes` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 4
          },
          "ya-lsp.rails.entrypoints": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Read mailers, jobs and Sidekiq workers, so `UserMailer.welcome` and `ImportJob.perform_later` are calls ya-lsp knows about. Overridden by `[rails] entrypoints` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 5
          },
          "ya-lsp.rails.views": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Answer inside a template: which helpers a bare word can call, and what the `@story` the controller assigned is. The one to turn off in a **Sinatra or Hanami** project with an `app/views/`, where the controller ya-lsp would cite does not exist. Overridden by `[rails] views` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 6
          }
        }
      },
      {
        "title": "Trees",
        "order": 8,
        "properties": {
          "ya-lsp.trees.test": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "spec",
              "test",
              "tests",
              "features"
            ],
            "markdownDescription": "Directory names that hold your test suite. A `def` written in one is offered and jumped to only from inside another — it exists while the suite is loaded and nowhere else. This **replaces** the list rather than adding to it, because a name here *removes* answers: add `lib` and your whole project stops completing. `[]` turns the fence off. Uses of a name are still found everywhere: `references`, `rename` and highlight never fence. Overridden by `[trees] test` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.trees.testSupport": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "markdownDescription": "Extra directory names that count as test scaffolding **for the cursor only** — somewhere a developer is, rather than somewhere answers are removed from. Added to the built-in `testing_support`, never replacing it, because a wrong name here costs nothing but the protection it was going to give. Overridden by `[trees] test_support` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.trees.migration": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "db/migrat"
            ],
            "markdownDescription": "Where migrations live, as `parent/mark` pairs: the directory above, then anything the directory's own name contains. `db/migrat` covers `db/migrate`, `db/post_migrate` and `db/old_migrations` in one rule. A migration is loaded by path, alone, by the task that runs it — so a backfill copy of a model written inside one is reachable from nothing. **Replaces** the pair; `[]` turns the fence off. Overridden by `[trees] migration` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          }
        }
      },
      {
        "title": "Indexing",
        "order": 9,
        "properties": {
          "ya-lsp.index.include": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "**/*.rb",
              "**/*.erb",
              "**/*.rbs",
              "**/*.rake",
              "**/*.gemspec",
              "**/Rakefile",
              "**/Gemfile",
              "**/config.ru"
            ],
            "markdownDescription": "Globs, relative to the workspace folder, of the files to index. Everything ya-lsp knows about your code comes from these, so narrowing it is the quickest way to make navigation stop working. The default covers Ruby that is not named `.rb` — your `Rakefile`, `Gemfile`, `config.ru`, `lib/tasks/*.rake` and your own `.gemspec` — and the RBS signatures in `sig/`. Overridden by `[index] include` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 1
          },
          "ya-lsp.index.exclude": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "vendor/**/*",
              ".bundle/**/*",
              "tmp/**/*",
              "node_modules/**/*"
            ],
            "markdownDescription": "Globs to skip, applied after `#ya-lsp.index.include#`. Where vendored or generated Ruby goes when git does not already ignore it. Overridden by `[index] exclude` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 2
          },
          "ya-lsp.index.loadPaths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "lib",
              "app"
            ],
            "markdownDescription": "Extra roots to index and to resolve `require \"...\"` against, relative to the workspace folder — the `$LOAD_PATH` ya-lsp assumes. `require \"foo/bar\"` reaches `lib/foo/bar.rb` because of this. Overridden by `[index] load_paths` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 3
          },
          "ya-lsp.index.respectGitignore": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Skip anything git ignores. Right for a code indexer by default — it prunes `vendor/bundle`, `node_modules` and `tmp` for free — but a project whose Ruby is generated into an ignored directory needs it off. Overridden by `[index] respect_gitignore` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 4
          },
          "ya-lsp.index.maxFiles": {
            "type": "integer",
            "default": 50000,
            "minimum": 1,
            "markdownDescription": "Refuse to index a workspace with more Ruby files than this, rather than thrashing. Raise it only after ya-lsp has said it stopped. Overridden by `[index] max_files` in a workspace `ya-lsp.toml`.",
            "scope": "resource",
            "order": 5
          }
        }
      }
    ],
    "languages": [
      {
        "id": "erb",
        "aliases": [
          "ERB",
          "Embedded Ruby"
        ],
        "extensions": [
          ".erb",
          ".rhtml"
        ]
      }
    ],
    "commands": [
      {
        "command": "ya-lsp.restart",
        "title": "Restart Server",
        "category": "ya-lsp"
      },
      {
        "command": "ya-lsp.showOutput",
        "title": "Show Output",
        "category": "ya-lsp"
      }
    ]
  },
  "scripts": {
    "compile": "node -e \"require('fs').rmSync('out', { recursive: true, force: true })\" && tsc -p .",
    "lint": "tsc -p . --noEmit",
    "bundle": "node esbuild.mjs",
    "test": "yarn compile && yarn bundle && node --test \"out/**/*.test.js\"",
    "stage-shared": "node -e \"for (const f of ['NOTICE.txt','THIRD-PARTY-NOTICES.txt','CHANGELOG.md']) require('fs').copyFileSync('../../'+f, f)\"",
    "vscode:prepublish": "yarn stage-shared && yarn bundle",
    "package": "vsce package"
  },
  "dependencies": {
    "vscode-languageclient": "10.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/vscode": "1.108.1",
    "@vscode/vsce": "^3.9.0",
    "esbuild": "^0.28.0",
    "typescript": "^7.0.0"
  }
}
